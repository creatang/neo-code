# **项目代号：Neocode - 你的终端原生 AI 诊断工程师**

## 1. 背景与痛点 (The Problem)

在日常的软件开发、环境配置或系统运维中，终端（Terminal）是我们交互最频繁的入口。然而，现有的 AI 辅助工具多停留在浏览器端或 IDE 侧。 当终端出现复杂报错（如编译失败、依赖冲突、环境异常）时，开发者需要手动复制杂乱的日志，切换应用进行提问。这不仅打断了心流，还常常因为遗漏了操作系统版本、路径等**关键上下文（Context）**，导致 AI 给出的答案不准确。

## 2. 产品愿景 (Our Solution)

Neocode 是一款运行在命令行的智能 CLI 工具。它能够静默感知或按需读取终端的上下文，将“报错定位 → 根因分析 → 修复方案”的闭环直接在终端内完成，打造沉浸式的开发体验。

## 3. 核心功能场景 (Core Scenarios)

- **场景 A：按需诊断 (Manual 模式 - 极简低噪)**
  - **触发条件：** 用户执行某条命令失败，看到报错后，手动输入 `neocode -debug`。
  - **执行逻辑：** 工具向上追溯，提取最近一次执行失败的命令及其输出日志（包括 stdout/stderr），结合系统环境信息，发送至大模型进行分析。
  - **用户体验：** 像呼叫一个在旁边看着你屏幕的资深同事，帮你快速解释刚才为什么挂了，并给出修复命令。
- **场景 B：全天候守护 (Auto 模式 - 极致自动化)**
  - **触发条件：** 用户通过 `neocode -debug -on` 开启监听模式。
  - **执行逻辑：** Neocode 转入后台代理/监听状态。一旦检测到后续命令非正常退出（Exit Code \\= 0）或抛出严重异常堆栈，立即自动介入。
  - **用户体验：** 报错的下一秒，屏幕上直接追加 AI 的高亮分析和建议，甚至可以直接按回车一键应用修复方案。



## 4. 技术方案

#### 方案 A：伪终端（Pseudo-Terminal, PTY）代理模式（推荐，最健壮）

这种方案下，`neocode` 实际上是一个终端的“外壳（Wrapper）”。

- **原理：** 当用户启动终端时，实际上是进入了 `neocode` 启动的一个 PTY 会话中（类似于 `tmux` 或 `script` 命令的原理）。用户的所有输入，以及 Shell 的所有输出（stdout, stderr），都会先经过 `neocode` 的内存，然后再渲染到屏幕上。
- **如何实现功能：**
  - **Auto 模式：** `neocode` 监听子进程（Shell）的输出。结合正则表达式和退出状态码（Exit Code），一旦发现明显的 Error/Exception 堆栈，立刻将最近 N 行输出丢给 AI 分析，并将结果插入到当前光标下方。
  - **Manual 模式：** `neocode` 在内存中维护一个环形缓冲区（Ring Buffer），保存最近 500 行的屏幕输出。当用户输入 `neocode -debug` 时，工具直接从缓冲区提取上一条命令的完整报错，发送给 AI。
- **优缺点：** 可以完美捕获所有输出，不依赖特定的 Shell（Bash/Zsh/PowerShell 都支持）；缺点是开发难度较大，需要处理复杂的终端控制字符（ANSI Escape Codes）和信号传递（如 Ctrl+C）。

#### 方案 B：Shell Hook 与流重定向（轻量级）

- **原理：** 不接管整个终端，而是通过修改用户的 `.bashrc` 或 `.zshrc`，注入钩子函数（如 Zsh 的 `preexec` 和 `precmd`）。
- **如何实现功能：**
  - 利用 Hook，在每次命令执行前，将标准错误流（stderr）重定向并追加到一个临时日志文件中（例如 `/tmp/neocode_last_error.log`）。
  - 当命令执行结束（`precmd` 触发）时，检查该命令的退出状态码 `$?`。如果 `$? != 0`，说明报错了。
  - **Auto 模式：** 如果开启了 `-on`，状态码非 0 时直接触发 Python/Go 脚本读取那个临时日志，调用 AI 并回显。
  - **Manual 模式：** 用户输入 `neocode -debug` 时，程序去读取那个临时日志并分析。
- **优缺点：** 实现相对简单，对终端的侵入性小，不会弄坏用户的 Vim/Nano 等全屏应用；缺点是有些程序不遵循规范，报错信息输出到了 stdout 而不是 stderr，可能会漏抓。

#### 方案 C：基于 Tmux / 终端模拟器 API 插件开发

- **原理：** 放弃做一个纯粹的 CLI 命令行工具，而是把它做成特定终端环境的插件。比如做一个 Tmux 插件，或者 iTerm2 / Windows Terminal 的插件。
- **如何实现功能：** Tmux 提供了 `capture-pane` 命令，可以直接截取当前终端屏幕上的所有文本内容。当你输入 `neocode -debug` 时，背后其实是调用终端的 API 抓取屏幕文字，正则匹配出最后一条命令及其报错，然后进行分析。
- **优缺点：** 开发极快，绕过了复杂的底层进程间通信和流处理；缺点是绑定了特定的生态（比如不装 Tmux 就用不了）。



------

## 5.个人想法——使用方案B

我个人推荐使用方案B，主要是为了轻量且开发难度低些许。

关于方案B实现**是一个终端对应一个临时日志（按进程/Session隔离）**。绝对不能全部终端共用一个日志文件。

### 一、 为什么不能共用一个日志？（共享的灾难）

如果你让所有终端共用一个 `/tmp/neocode_last_error.log`，会立刻面临以下几个致命问题：

1. **上下文混乱（Context Confusion）：** 设想用户在终端 A 跑着一个前端 npm 项目报错了，同时在终端 B 跑着一个后端 Python 脚本也报错了。如果共用一个日志，终端 B 的报错会覆盖终端 A 的报错。此时用户在终端 A 输入 `neocode -debug`，AI 获取到的却是 Python 的错误日志，给出的解决方案将完全“牛头不对马嘴”。
2. **竞态条件（Race Condition）：** 多个终端进程（Shell）并发向同一个文件写入数据，如果没有实现复杂的文件锁（File Lock）机制，日志内容极易发生交错和损坏。
3. **状态残留：** 终端 C 已经闲置了几个小时****，用户突然敲 `neocode -debug`，却把终端 D 刚刚产生的报错发给了 AI。

### 二、 如何优雅地实现“一端一日志”？

最佳实践是利用操作系统层面的**进程 ID（PID）**来进行隔离。每一个打开的终端窗口或标签页，在底层都是一个独立的 Shell 进程（拥有唯一的 PID）。

**具体的实现逻辑如下：**

- **1. 日志命名与绑定：** 在 Shell 脚本（你的 Hook 代码）中，可以通过特殊变量 `$$` 获取当前 Shell 进程的 PID。 你可以将日志文件命名为：`/tmp/neocode_error_$$.log`。 *这样，终端 A 的 PID 是 1001，日志就是 ..._1001.log；终端 B 是 1002，日志就是 ..._1002.log。互不干扰。*
- **2. 写入与读取：**
  - **执行命令时：** 钩子函数将错误输出重定向到 `/tmp/neocode_error_$$.log`。
  - **调用 AI 时：** 当用户在当前终端输入 `neocode -debug`，你的 CLI 程序需要获取其父进程（也就是当前 Shell）的 PID，然后去精准读取对应的 `/tmp/neocode_error_<父进程PID>.log`。
- **3. 生命周期与垃圾回收（极其重要）：** 临时文件不能无限期堆积在用户的硬盘里。你需要在 Hook 中利用 Shell 的 `trap` 机制。当终端正常关闭（触发 EXIT 信号）时，自动删除属于该终端的日志文件。