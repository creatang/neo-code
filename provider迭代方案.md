## 现状
* 供应商配置信息存储：目前是写死在了config层下builtin_providers，包括供应商url、使用的apikey环境变量名、模型列表等等
* 供应商与模型切换：当前选中的provider和model信息是放在了yaml，经由service-》manager-》loader序列化并将新的信息存回yaml



## 问题
* 用户无法自行修改供应商信息和模型信息，这部分都是由开发者自己写死在config下的
* 模型信息不全面：当前将模型信息交由开发者写死，后期如果供应商模型信息有变，就得由开发者自行维护
* 目前apikey是存在了环境变量，且不同的provider会使用不同的环境变量名，最大的风险是到了生产开发后，环境变量可能因为日志、进程列表等信息导致环境变量泄露。若不考虑安全性，暂时使用命令行将用户的apikey存到相应的环境变量
* 将provider信息存入yaml后，如果发生了开发者删除provider或是供应商不再供应该模型的情况，将会导致当前项目运行出错

## 方案
* 供应商信息并不写死在config中，而是改用SQLite存储，这样方便用户存储自定义的供应商信息
* 模型信息不写死在config中，而是使用v1/models 定时/切换provider时 主动获取新的模型列表信息（还包括具体的最大token、支持功能等信息）。
* 目前有三种方案：（1）凭据管理器：不同操作系统的凭据管理器都是不一样的，使用它可能还有为此额外做适配  （2）SQLite：加密后存储入sqlite，目前最考虑的方案   （3）集中式密钥管理服务：保密性强，但当前项目似乎只有apikey有加密存储的必要
* 有关yaml信息存储还需要调研方案