# NeoCode 多模态激进重构方案

> 版本: v4.0 - 纯文本版  
> 日期: 2026-04-06  
> 状态: 待评审

---

## 1. 当前架构核心问题

### 1.1 根本性瓶颈

| 层级              | 当前设计               | 问题                             |
| ----------------- | ---------------------- | -------------------------------- |
| **Message**       | Content 字段为纯字符串 | 锁定纯文本范式，无法扩展多模态   |
| **ChatRequest**   | 纯文本消息数组         | 无法表达图片、文件等多模态内容   |
| **UserInput**     | Content 字段为纯字符串 | 无法携带附件                     |
| **StreamEvent**   | 使用指针和类型断言     | 无编译时安全，需要到处判空       |
| **Provider 接口** | 无能力协商机制         | 无法知道 Provider 是否支持多模态 |

### 1.2 重构目标

**从"纯文本优先"到"多模态原生"**：

- 文本只是多模态的一种类型
- 所有消息统一使用内容片段数组
- 零兼容包袱，全量迁移

---

## 2. 核心类型重构

### 2.1 消息模型重写

**改造文件**: `provider/types.go`

**核心变更**:

- 新增内容类型枚举，包含文本、图片、文件、音频、视频五种类型
- 新增内容片段结构体，包含类型、文本数据、二进制数据、MIME 类型、URL、扩展元数据等字段
- Message 结构的 Content 字段从字符串改为内容片段数组
- 新增消息角色枚举，包含系统、用户、助手、工具四种角色
- 消息结构新增 ID、时间戳字段

### 2.2 事件系统重构

**改造文件**: `provider/types.go`

**核心变更**:

- 废弃当前的指针+判空模式
- 采用 Discriminated Union 风格的事件设计
- 定义事件接口，所有具体事件实现此接口
- 新增事件类型枚举，包含文本增量、图片增量、文件增量、工具调用开始/增量/完成、消息完成、错误等类型
- 为每种事件类型定义独立的结构体，包含类型专属字段和时间戳
- 事件流使用类型安全的通道

### 2.3 Provider 接口扩展

**改造文件**: `provider/provider.go`

**核心变更**:

- Chat 方法返回值从错误改为事件通道和错误
- 新增 Capabilities 方法，用于能力协商
- 新增 ImageDescribe 方法，用于图片描述（可选）
- 新增能力结构，包含多模态能力、速率限制、模型信息等
- 新增多模态能力结构，包含图片/文件/音频支持、大小限制、格式支持、单消息图片数量限制等
- ChatRequest 结构使用新的多模态消息类型

### 2.4 Runtime 层重构

**改造文件**: `runtime/runtime.go`

**核心变更**:

- UserInput 结构新增附件列表字段
- 新增附件结构，包含类型、本地路径、MIME 类型、名称、二进制数据等字段
- 新增附件类型枚举，包含图片、文件、音频、视频四种类型
- 消息组装逻辑重写，将文本和附件统一转换为内容片段数组
- 新增空消息检查，确保消息不为空

---

## 3. Provider 实现适配

### 3.1 OpenAI Provider

**改造文件**: `provider/openai/chat.go`

**核心变更**:

- 消息构建逻辑重写，检测单文本内容时使用简洁格式，多模态内容时使用 MultiContent 格式
- 新增多内容构建函数，将内容片段数组映射为 OpenAI API 格式
- 图片内容编码为 base64 Data URI 格式

### 3.2 Anthropic Provider

**改造文件**: `provider/anthropic/chat.go`

**核心变更**:

- 消息构建逻辑重写，将内容片段映射为 Anthropic 的 content blocks
- 文本内容使用文本消息内容函数
- 图片内容解析 Data URI 后使用图片消息内容函数

---

## 4. TUI 层重构

### 4.1 输入层改造

**改造文件**: `tui/core/app/input_features.go`

**核心变更**:

- 粘贴检测逻辑扩展，支持图片粘贴检测
- 图片粘贴时保存到临时目录并添加到附件列表
- 输入框显示附件引用标记
- 新增文件拖拽支持

### 4.2 状态管理

**改造文件**: `tui/state/ui_state.go`

**核心变更**:

- UIState 结构新增附件列表字段
- 新增附件预览数据映射字段

### 4.3 渲染层改造

**改造文件**: `tui/core/app/view.go`

**核心变更**:

- 用户消息渲染逻辑重写，遍历内容片段数组
- 根据内容类型显示不同标识：文本直接显示，图片显示图片标识，文件显示文件类型
- 新增附件栏组件，显示已添加的附件列表
- 新增图片预览组件

---

## 5. 降级策略

### 5.1 能力协商

**改造文件**: `runtime/runtime.go`

**核心变更**:

- 发送前检查 Provider 能力
- 检测是否有图片附件
- 能力不匹配时触发降级策略

### 5.2 降级策略选项

| 策略             | 触发条件                      | 行为                 |
| ---------------- | ----------------------------- | -------------------- |
| **OCR 转换**     | 模型不支持图片 + OCR 可用     | 图片转文字后发送     |
| **视觉模型转换** | 模型不支持图片 + 视觉模型可用 | 调用视觉模型生成描述 |
| **提示切换**     | 有替代模型可用                | TUI 显示选择菜单     |
| **拒绝发送**     | 无任何降级手段                | 返回错误提示         |

---

## 6. 图片处理

### 6.1 编码策略

| Provider      | 编码方式      | 大小限制 | 格式支持             |
| ------------- | ------------- | -------- | -------------------- |
| **OpenAI**    | base64 或 URL | 20MB     | PNG, JPEG, WebP, GIF |
| **Anthropic** | base64        | 5MB      | PNG, JPEG, WebP, GIF |
| **Gemini**    | inline_data   | 20MB     | PNG, JPEG, WebP      |

### 6.2 处理流程

图片输入 → 检测格式 → 检查 Provider 支持情况 → 检查大小 → 编码为 base64 → 添加到内容片段数组

**压缩策略**:

- 尺寸超过限制时等比缩放
- 文件大小超过限制时 JPEG 压缩
- 格式不支持时转换为 PNG

---

## 7. 实施路线

### Phase 1: 核心类型重构

| 任务                                | 文件                   |
| ----------------------------------- | ---------------------- |
| 重写 Message.Content 为内容片段数组 | `provider/types.go`    |
| 重构事件系统为 Discriminated Union  | `provider/types.go`    |
| 扩展 Provider 接口，新增能力协商    | `provider/provider.go` |

### Phase 2: Provider 适配

| 任务                          | 文件                         |
| ----------------------------- | ---------------------------- |
| OpenAI Provider 多模态适配    | `provider/openai/chat.go`    |
| Anthropic Provider 多模态适配 | `provider/anthropic/chat.go` |
| 能力协商实现                  | `provider/*/capabilities.go` |

### Phase 3: Runtime 层重构

| 任务                         | 文件                         |
| ---------------------------- | ---------------------------- |
| UserInput 扩展，新增附件列表 | `runtime/runtime.go`         |
| 消息组装逻辑重写             | `runtime/runtime.go`         |
| 图片处理（编码/压缩）        | `runtime/image.go` (新增)    |
| 降级策略实现                 | `runtime/fallback.go` (新增) |

### Phase 4: TUI 层重构

| 任务           | 文件                                      |
| -------------- | ----------------------------------------- |
| 图片粘贴检测   | `tui/core/app/input_features.go`          |
| 附件状态管理   | `tui/state/ui_state.go`                   |
| 附件栏组件     | `tui/components/attachment_bar.go` (新增) |
| 多模态消息渲染 | `tui/core/app/view.go`                    |

---

## 8. 依赖变更

| 依赖          | 用途                  | 是否新增 |
| ------------- | --------------------- | -------- |
| 图片标准库    | 图片解码              | 否       |
| base64 标准库 | 图片编码              | 否       |
| imaging 库    | 图片处理（缩放/压缩） | 是       |
| gosseract 库  | Tesseract OCR 封装    | 可选     |

---

## 9. 风险与缓解

| 风险                 | 影响       | 缓解措施                          |
| -------------------- | ---------- | --------------------------------- |
| 大图片导致上下文溢出 | 请求失败   | 压缩 + 大小限制 + 数量限制        |
| Provider 不支持图片  | 功能不可用 | 能力检测 + 降级策略               |
| 剪贴板兼容性问题     | 粘贴失败   | 多格式检测                        |
| 内存泄漏             | 性能下降   | 临时文件自动清理 + 会话结束时删除 |
| OCR 质量差           | 用户体验差 | 混合策略（OCR → 视觉模型 → 提示） |

---

## 10. 总结

| 维度         | 方案                                     |
| ------------ | ---------------------------------------- |
| **核心改造** | Message.Content 从字符串改为内容片段数组 |
| **事件系统** | 指针+判空 → Discriminated Union          |
| **设计理念** | 全多模态原生，文本只是多模态的一种类型   |
| **兼容策略** | 零兼容，全量迁移                         |
| **降级策略** | OCR → 视觉模型 → 提示切换 → 拒绝         |

**核心优势**:

1. 架构统一，无历史包袱
2. 类型安全，编译时保证
3. 多模态原生，非补丁式扩展
4. 代码简洁，单一逻辑路径
5. 未来扩展极易（新增内容类型即可）
