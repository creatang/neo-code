## Codex、OpenCode 与 Claude Code 安全机制异同对比表

| **比较维度**       | **Codex**                                                    | **OpenCode**                                                 | **Claude Code**                                              |
| ------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **核心安全架构**   | **沙盒 + 审批双层解耦**：沙盒决定技术边界，审批决定何时请求确认。 | **纯细粒度权限配置**：通过配置决定操作是自动运行、提示审批还是被阻止。 | **分层权限 + OS级沙盒**：权限系统控制工具可用性，沙盒专门限制 Bash 及其子进程的系统访问。 |
| **权限评估优先级** | 提供预设组合（如自动、只读、全量访问），可配置细粒度（granular）审批策略。 | **最后匹配优先**：采用模式匹配，最后匹配的规则优先执行，支持代理（Agent）级别独立覆盖且优先。 | **Deny 始终优先**：严格按照 `deny -> ask -> allow` 顺序评估，高优先级 deny 无法被 allow 覆盖。 |
| **OS 级沙盒实现**  | **依赖系统原生机制**：macOS 用 Seatbelt，Linux 用 bubblewrap+seccomp，Windows 用原生沙盒或 WSL。 | **无底层沙盒**：未提及操作系统级隔离，依赖 `external_directory` 配置限制工作区外的路径访问。 | **依赖系统原生机制**：macOS 用 Seatbelt，Linux/WSL2 用 bubblewrap（不支持 WSL1）。 |
| **网络与防注入**   | 默认关闭网络，网页搜索默认使用**缓存索引（cached）**以降低抓取恶意指令的风险。 | 通过 `webfetch`、`websearch` / `codesearch` 权限配置来拦截或放行。 | 沙箱外挂**代理服务器**控制域名访问，指出允许宽泛域名（如 github.com）仍有数据泄露风险。 |
| **文件与目录保护** | 默认在可写工作区中强制保护 `.git`、`.agents`、`.codex` 为递归只读。 | 默认允许 read，但强制拦截 `.env` 及其相关文件（除 `.env.example`）的读取。 | 读写规则遵循 gitignore 规范的四种路径模式，支持绝对路径与相对路径的隔离。 |
| **异常防护机制**   | 建议配合版本控制，频繁提交以便小步回滚。                     | **防死循环 (doom_loop)**：同一工具相同输入重复3次触发审批。  | 提供**逃生舱机制**：不兼容沙箱的工具可禁用沙箱重试；支持 `PreToolUse` hooks 扩展权限评估。 |



## 安全机制调研

### Codex：[[Agent approvals & security](https://developers.openai.com/codex/agent-approvals-security#/)](https://developers.openai.com/codex/agent-approvals-security#/)

**1. 核心安全理念：沙盒与审批**

- **内容**：
  - Codex 的安全操作依赖两个协同工作的层级：
    - **“沙盒模式（Sandbox mode）”**决定了 Codex 在技术上能做什么（例如写入文件或访问网络的权限）；
    - **“审批策略（Approval policy）”**决定了 Codex 在执行操作（如离开沙盒、使用网络等）前何时必须停下来请求用户的同意。
  - 云端（Codex cloud）运行在 OpenAI 托管的隔离容器中；
  - 而本地（CLI / IDE 扩展）则依赖操作系统级别的机制来执行沙盒策略。
- **文章位置**：*Agent approvals & security* 及 *Sandbox and approvals* 部分。

**2. 网络访问与安全风险控制**

- **内容**：在本地默认状态下，Codex 的网络访问是关闭的。如果启用了网络访问或网页搜索功能，可能会带来提示词注入（Prompt injection）风险，导致代理抓取并执行不受信任的指令。为降低风险，网页搜索工具默认使用 OpenAI 维护的网页结果缓存索引（`cached`），而非直接获取实时网页。用户也可以手动配置开启实时搜索（`live`）或禁用该工具（`disabled`）。
- **文章位置**：*Network access* 部分。

**3. 默认启动配置与敏感目录保护**

- **内容**：Codex 启动时会根据目录情况自动推荐配置：如果是受版本控制（如 Git）的文件夹，默认进入 Auto 模式（允许工作区写入 + 按需请求审批）；如果是非受版本控制的文件夹，默认进入只读模式。此外，在可写的工作区中，某些特定路径会被强制保护为递归“只读”状态，包括 `.git`、`.agents` 和 `.codex` 目录。
- **文章位置**：*Defaults and recommendations* 及包含的 *Protected paths in writable roots* 部分。

**4. 审批配置与常用权限组合**

- **内容**：用户可以通过命令标志（如 `--ask-for-approval never`）来调整或完全关闭审批提示。文章列出了一些常见的意图与权限组合，例如：完全自动（Auto/ `--full-auto`）、安全只读（只答疑不修改）、CI非交互只读、只对不受信任的命令请求审批、以及带有极高风险的危险全量访问模式（使用 `--yolo` 标志，禁用沙盒和审批）。用户也可以在 `config.toml` 中配置细粒度（granular）的审批策略并保存为特定配置集（profiles）。
- **文章位置**：*Run without approval prompts*、*Common sandbox and approval combinations* 及 *Configuration in config.toml* 部分。

**5. 操作系统层面的沙盒实现**

- **内容**：沙盒机制在不同的操作系统中实现方式不同。macOS 使用 Seatbelt 策略；Linux 默认使用 bubblewrap 管道加上 seccomp；Windows 运行时原生使用 Windows 沙盒，但在 VS Code 扩展中，支持强制让代理在 WSL（Windows Subsystem for Linux）内运行以继承 Linux 的沙盒语义。如果在 Docker 容器中由于宿主机不支持沙盒特性而无法运行，建议在容器层面做好隔离后，使用危险全权限标志运行 Codex。
- **文章位置**：*Test the sandbox locally* 与 *OS-level sandbox* 部分。

**6. 配合版本控制的最佳实践**

- **内容**：文章建议配合版本控制（如 Git）来使用 Codex，从而保证代码修改容易隔离和撤销。建议的做法包括：在特性分支上工作并保持干净的工作区、偏好使用基于补丁的工作流（如 `git diff / git apply`）而不是直接编辑跟踪的文件、频繁提交以便小步回滚，以及像审查同事的 PR（拉取请求）一样去验证和审查 Codex 的修改建议。
- **文章位置**：*Version control* 部分。

**7. 监控、遥测（Telemetry）与隐私指引**

- **内容**：为了满足审计和排查需求，Codex 支持通过 OpenTelemetry (OTel) 进行系统监控，但该功能默认是关闭（Opt-in）的，以确保本地运行的数据封闭性。如果启用，它会记录 API 请求、工具调用决策、WebSocket/SSE 活动等事件。出于安全与隐私考量，配置默认开启“脱敏”（不记录用户的具体提示词和敏感内容），官方建议仅将遥测数据路由到用户自己控制的收集器，并对工具的输入输出作为敏感信息进行处理。
- **文章位置**：*Monitoring and telemetry* 及其子部分（*Overview*, *Enable OTel*, *Event categories*, *Security and privacy guidance*）。

**8. 企业托管配置**

- **内容**：企业管理员可以通过托管配置功能（Managed configuration），为其团队的工作区统一设置 Codex 的安全和策略要求。
- **文章位置**：*Managed configuration* 部分。

------

### [[Codex Security](https://developers.openai.com/codex/security#/)](https://developers.openai.com/codex/security#/)

**1. 产品概述与核心价值**

- **内容**：Codex Security 是一款帮助工程和安全团队在已连接的 GitHub 仓库中发现、验证并修复潜在安全漏洞的产品。它主要通过以下三种方式帮助团队：
  1. 利用特定于仓库的威胁模型（threat model）和真实代码上下文来发现潜在漏洞。
  2. 在人工审查之前对发现的问题进行验证，从而减少干扰信息（噪音）。
  3. 通过提供排名结果、证据和建议的补丁选项，推动漏洞的修复进程。
- **文章位置**：页面顶部的引言部分（*Codex Security* 标题下方）。

**2. 工作原理 (How it works)**

- **内容**：Codex Security 会逐个提交（commit by commit）地扫描已连接的仓库。它会根据你的仓库构建扫描上下文，对照该上下文检查潜在的漏洞，并在将这些高信号（high-signal）问题展示给你之前，在一个隔离的环境中对它们进行验证。 该工作流主要侧重于：
  - 特定于仓库的上下文（而不是通用的签名）。
  - 帮助减少误报（false positives）的验证证据。
  - 可以在 GitHub 中审查的修复建议。
- **文章位置**：*How it works*（工作原理）部分。

**3. 访问权限与先决条件 (Access and prerequisites)**

- **内容**：Codex Security 通过 Codex Web 与已连接的 GitHub 仓库协同工作，其访问权限由 OpenAI 管理。如果你需要访问权限或发现某个仓库不可见，需要联系你的 OpenAI 客户团队，并确认该仓库在你的 Codex Web 工作区中是可用的。
- **文章位置**：*Access and prerequisites*（访问权限与先决条件）部分。

**4. 相关文档与指引 (Related docs)**

- **内容**：文章最后列出了相关的参考文档资源：
  - **Setup（设置）**：涵盖了产品的设置、扫描和发现结果审查。
  - **FAQ（常见问题解答）**：涵盖了常见的商品问题。
  - **Improving the threat model（改进威胁模型）**：解释了如何调整范围、攻击面以及严重性假设。
- **文章位置**：*Related docs*（相关文档）部分。

------

### Codex：[[Sandboxing](https://developers.openai.com/codex/concepts/sandboxing#/)](https://developers.openai.com/codex/concepts/sandboxing#/)

**1. 沙盒的核心概念与作用**

- **内容**：沙盒是一种边界机制，它允许 Codex 在不获取机器无限制访问权限的情况下自主行动。在 Codex 应用程序、IDE 扩展或 CLI 中运行本地命令时，这些命令会在受限环境中执行，而不是默认拥有完全访问权限。沙盒定义了技术边界（例如可以修改哪些文件、是否可以使用网络），当任务在边界内时，Codex 可以持续运行而无需确认；当需要超出边界时，则退回到审批流程（approval flow）。沙盒与审批是协同工作的不同控制手段。
- **文章位置**：*Introduction (引言部分)*

**2. 沙盒的执行范围与底层实现**

- **内容**：沙盒不仅适用于 Codex 内置的文件操作，也适用于其生成的命令。如果 Codex 运行诸如 `git`、包管理器或测试运行器等工具，这些命令同样继承相同的沙盒边界。Codex 在不同操作系统（macOS、Linux、WSL 和原生 Windows）上使用平台原生的执行机制来实现沙盒，核心理念都是为代理（agent）提供一个有边界的工作区域。
- **文章位置**：*What the sandbox does（沙盒的作用）* 部分。

**3. 沙盒的重要意义**

- **内容**：沙盒能够减少“审批疲劳”（approval fatigue）。由于日常的项目命令和文件读写操作在预先批准的边界内自动进行，系统不需要对每个低风险命令都请求用户确认。这也为代理化（agentic）工作提供了更清晰的信任模型，因为用户知道代理是在强制限制内运行，从而能更放心地让 Codex 独立工作。
- **文章位置**：*Why it matters（为什么它很重要）* 部分。

**4. 沙盒权限的控制方式**

- **内容**：
  - 在 **Codex 应用和 IDE** 中，用户可以通过聊天输入框或编辑器下方的权限选择器（permissions selector）来选择模式，例如默认权限、完全访问权限或自定义配置。
  - 在 **CLI (命令行)** 中，用户可以在会话期间使用 `/permissions` 命令来切换模式。
- **文章位置**：*How you control it（如何控制它）* 部分。

**5. 默认配置与常见模式**

- **内容**：用户可以通过修改本地设置文件 `config.toml` 来配置默认的沙盒与审批行为。
  - **常见的沙盒模式（Sandbox modes）**：
    - `read-only`（只读）：Codex 可以检查文件，但未经批准不能编辑文件或运行命令。
    - `workspace-write`（工作区可写）：Codex 可以在工作区内读取文件、编辑文件并运行日常本地命令，这是本地工作默认的低摩擦模式。
    - `danger-full-access`（危险-完全访问）：Codex 在没有沙盒限制的情况下运行，移除文件系统和网络边界，仅在希望赋予完全访问权限时使用。
  - **常见的审批策略（Approval policies）**：
    - `untrusted`：在运行非信任集内的命令前进行询问。
    - `on-request`：默认在沙盒内工作，需要超出边界时进行询问。
    - `never`：永远不停止并请求审批。
- **文章位置**：*Configure defaults（配置默认值）* 部分。

**6. 高级权限配置与异常处理**

- **内容**：
  - “完全访问”意味着组合使用 `sandbox_mode = "danger-full-access"` 和 `approval_policy = "never"`；而 `--full-auto` 是一种低风险的本地自动化预设（组合了 `workspace-write` 和 `on-request`）。
  - 如果 Codex 需要在多个目录工作，可以通过配置 **可写根目录（writable roots）** 来扩展权限，而不是完全移除沙盒。
  - 当工作流需要特定例外时，应使用 **规则（rules）** 来允许、提示或禁止沙盒外的特定命令前缀，这比广泛扩大访问权限更合适。
- **文章位置**：*Configure defaults（配置默认值）* 部分的中后段。



------

------

### [[权限 | OpenCode](https://opencode.ai/docs/zh-cn/permissions/#/)](https://opencode.ai/docs/zh-cn/permissions/#/)

**1. 权限控制概述与演进**

- **内容**：OpenCode 使用 `permission` 配置来决定某个操作是应自动运行、提示用户审批，还是被直接阻止。从 v1.1.1 版本开始，旧版的 `tools` 布尔配置已被弃用并合并到 `permission` 中，但为了向后兼容，旧版配置仍被支持。
- **文章位置**：*权限 (页面顶部引言部分)*

**2. 权限规则的三种操作类型**

- **内容**：每条权限规则会解析为三种操作之一：
  - `"allow"`：无需审批直接运行。
  - `"ask"`：提示用户审批。
  - `"deny"`：阻止该操作。
- **文章位置**：*操作 (操作)* 部分

**3. 全局与特定工具的配置方法**

- **内容**：可以通过配置文件（`opencode.json`）全局设置权限（使用 `*`）或一次性设置所有权限，也可以覆盖特定工具的权限（如针对 `bash` 允许，针对 `edit` 拒绝）。
- **文章位置**：*配置 (配置)* 部分

**4. 细粒度规则与模式匹配 (对象语法)**

- **内容**：对于大多数权限，可以使用对象语法根据工具的输入来应用不同的操作。例如针对 `bash` 命令，可以配置拦截所有命令（`*`: `"ask"`），但放行 `git *` 和 `npm *`。规则评估采用**模式匹配**，最后匹配的规则优先（建议将通配符 `*` 规则放在最前面，具体规则放在后面）。
- **文章位置**：*细粒度规则（对象语法）* 部分

**5. 路径与匹配规则的特殊语法**

- **内容**：
  - **通配符**：`*` 匹配零个或多个字符，`?` 精确匹配一个字符。
  - **主目录展开**：模式开头可以使用 `~` 或 `$HOME` 来引用主目录（如 `~/projects/*`）。
  - **外部目录 (external_directory)**：用于允许工具访问 OpenCode 启动工作目录之外的路径。允许后，外部目录会继承当前工作空间的默认权限（如默认允许 `read`），如果需要限制特定操作（如拒绝 `edit`），需添加显式规则。
- **文章位置**：*通配符*, *主目录展开*, 以及 *外部目录* 部分

**6. 支持的可用权限类型**

- **内容**：权限配置以工具名称为键，外加安全防护项，主要包括：
  - `read`, `edit`, `glob`, `grep`, `list`, `bash`（运行 shell 命令），`task`（启动子代理），`skill`，`lsp`（暂不支持细粒度），`webfetch`，`websearch`/`codesearch`。
  - 额外的安全项：`external_directory`（访问工作目录外的路径时触发）和 `doom_loop`（同一工具以相同输入重复调用 3 次时触发）。
- **文章位置**：*可用权限* 部分

**7. 系统的默认权限设置**

- **内容**：如果未指定配置，系统采用宽松的默认值：大多数权限默认为 `"allow"`；`doom_loop` 和 `external_directory` 默认为 `"ask"`。`read` 默认为 `"allow"`，但 `.env` 及其相关文件（如 `*.env.*`）默认被拒绝，只有 `*.env.example` 例外被允许。
- **文章位置**：*默认值* 部分

**8. “Ask” 提示时的三种用户选择**

- **内容**：当 OpenCode 提示审批时，界面提供三种选择：
  - `once`：仅批准本次请求。
  - `always`：批准与建议模式匹配的后续请求（在当前会话剩余时间内有效，通常会将安全的命令前缀加入白名单）。
  - `reject`：拒绝请求。
- **文章位置**：*“Ask”的作用* 部分

**9. 代理 (Agent) 级别的独立权限覆盖**

- **内容**：用户可以为每个代理单独覆盖权限（配置在 `opencode.json` 的 `agent` 层级或 Markdown 配置文件中）。代理的权限会与全局配置合并，且**代理规则优先**。在带参数命令配置时，如 `"grep *"` 允许带参数执行，而仅写 `"grep"` 会阻止带参数执行。
- **文章位置**：*代理* 及 *提示 (Tip)* 部分

------

------

### [[配置权限 (Claude Code Docs)](https://code.claude.com/docs/zh-CN/permissions#/)](https://code.claude.com/docs/zh-CN/permissions#/)

**1. 权限系统概述与管理**

- **内容**：Claude Code 使用分层的权限系统来平衡功能和安全性。不同操作（如读取文件、执行 Bash 命令、修改文件）需要不同程度的批准（如 Bash 命令的永久批准、文件修改直到会话结束）。用户可以通过 `/permissions` UI 查看和管理权限。规则分为 `allow`（允许运行）、`ask`（提示确认）、`deny`（阻止运行），评估顺序为 deny -> ask -> allow，`deny` 规则始终优先。
- **文章位置**：*权限系统* 和 *管理权限* 部分。

**2. 权限模式 (Permission Modes)**

- **内容**：用户可以在设置文件中配置 `defaultMode`。支持的模式包括：
  - `default`：首次使用工具时提示权限。
  - `acceptEdits`：自动接受会话的文件编辑权限。
  - `plan`：仅分析，不能修改文件或执行命令。
  - `auto`：自动批准工具调用并在后台进行安全检查。
  - `dontAsk`：自动拒绝工具，除非通过规则预先批准。
  - `bypassPermissions`：跳过权限提示（对 `.git`、`.claude` 等敏感目录除外），仅建议在容器等隔离环境中使用。可通过托管设置禁用该模式。
- **文章位置**：*权限模式* 部分。

**3. 权限规则的语法与通配符**

- **内容**：权限规则格式为 `Tool` 或 `Tool(specifier)`。使用无括号的工具名称可匹配其所有使用情况（如 `Bash` 匹配所有 Bash 命令）。可以在括号中添加说明符（如 `Bash(npm run build)` 或 `Read(./.env)`）。Bash 规则支持使用 `*` 作为通配符（如 `Bash(* --version)`），当 `*` 在末尾且前有空格时，强制执行单词边界匹配。
- **文章位置**：*权限规则语法*、*匹配工具的所有使用*、*使用说明符进行细粒度控制* 和 *通配符模式* 部分。

**4. 特定工具的权限细则**

- **内容**：
  - **Bash**：复合命令（如含 `&&`）会被拆分并分别保存独立规则。尝试约束 Bash 中的 URL 参数是脆弱的（可通过变量或重定向绕过），建议结合阻止网络命令、使用 `WebFetch` 白名单或使用 Hooks 来更可靠地过滤。
  - **Read 和 Edit**：Edit 规则适用于编辑工具，Read 适用于读取工具。这两种规则遵循 gitignore 规范的四种路径模式（绝对路径以 `//` 开头，主目录以 `~/` 开头等）。deny 规则对 Bash 子进程无效。
  - **WebFetch、MCP 与 Agent**：可分别针对域名、MCP 服务器工具以及特定子代理（如 Explore、Plan）设置权限规则。
- **文章位置**：*工具特定的权限规则* 及其子部分（*Bash*、*Read 和Edit*、*WebFetch*、*MCP*、*Agent（subagents）*）。

**5. 权限的扩展与工作目录**

- **内容**：
  - **Hooks**：可以使用 PreToolUse hooks 在运行时执行权限评估（验证或拒绝特定命令）。Hook 返回 "allow" 仍受 deny 规则约束，而 Hook 的阻止（退出代码 2）优先于 allow 规则。
  - **工作目录**：Claude 默认可访问启动目录的文件，但可以通过 CLI 参数（`--add-dir`）、会话命令（`/add-dir`）或设置文件（`additionalDirectories`）来扩展对其他目录的访问权限。
- **文章位置**：*使用hooks 扩展权限* 和 *工作目录* 部分。

**6. 权限与沙箱的交互**

- **内容**：权限和沙箱是互补的层级：权限控制可以使用哪些工具和访问哪些文件，而沙箱（仅适用于 Bash 及子进程）提供 OS 级别的强制限制。两者结合可实现深度防御。
- **文章位置**：*权限如何与沙箱交互* 部分。

**7. 托管设置与自动模式分类器**

- **内容**：
  - **托管设置**：管理员可以部署无法被用户覆盖的托管设置（如 `allowManagedPermissionRulesOnly`、`blockedMarketplaces` 等），集中控制权限。
  - **自动模式 (autoMode)**：配置分类器以决定哪些操作可自动安全运行。通过设置 `autoMode.environment` 定义组织信任的基础设施（如受信任的域和存储桶）。可通过 `soft_deny` 和 `allow` 字段覆盖分类器的内置拦截和允许规则（注意：替换这部分将覆盖全部默认值，建议先通过命令导出默认值再修改）。可以使用 `claude auto-mode` 的命令来检查默认值、配置并获取AI反馈。
- **文章位置**：*托管设置*、*仅托管设置*、*配置自动模式分类器*、*定义信任的基础设施*、*覆盖阻止和allow 规则* 和 *检查默认值和您的有效配置* 部分。

**8. 设置的优先级**

- **内容**：权限规则的优先级从高到低依次为：托管设置 > 命令行参数 > 本地项目设置 > 共享项目设置 > 用户设置。高优先级的 deny 规则无法被低优先级的 allow 规则覆盖。
- **文章位置**：*设置优先级* 部分。

------

### [[Sandboxing (Claude Code Docs)](https://code.claude.com/docs/zh-CN/sandboxing)](https://code.claude.com/docs/zh-CN/sandboxing)

**1. 沙箱的作用与重要性**

- **内容**：Claude Code 的沙箱功能为代理执行（特别是 bash 工具）提供了一个更安全的环境。它通过操作系统级别的机制来强制执行文件系统和网络的隔离。这种机制解决了传统基于权限审批所带来的“批准疲劳”（用户频繁点击批准导致注意力下降）、生产力降低（频繁中断工作流）和自主性受限（代理等待审批时无法工作）等问题，使 Claude 能够在预定安全边界内更独立、高效地运行。
- **文章位置**：*概述* 及 *为什么沙箱很重要* 部分。

**2. 隔离机制的工作原理**

- **内容**：
  - **文件系统隔离**：默认情况下，沙箱命令只对当前工作目录及其子目录有读写权限，对电脑其余部分只有读取权限（部分受限目录除外）。不能在没有明确权限的情况下修改工作区外的文件。
  - **网络隔离**：网络访问通过沙箱外的代理服务器控制。仅能访问已批准的域名，新的域名请求会触发权限提示。
  - **操作系统级强制执行**：沙箱机制依赖于操作系统原语。macOS 使用 Seatbelt；Linux 和 WSL2 使用 bubblewrap（不支持 WSL1）。这些限制适用于所有产生的子进程（如 `kubectl`, `npm` 等）。
- **文章位置**：*工作原理* 及其子部分（*文件系统隔离*、*网络隔离*、*操作系统级强制执行*）。

**3. 如何启用与沙箱模式**

- **内容**：
  - **启用**：在 macOS 上开箱即用；在 Linux/WSL2 上需要预先安装 `bubblewrap` 和 `socat`。可通过 `/sandbox` 命令开启菜单。如果依赖缺失，默认会显示警告并在无沙箱下运行；若想将其设为硬失败（针对托管部署），可将 `sandbox.failIfUnavailable` 设为 `true`。
  - **沙箱模式**：分为“自动允许模式”（命令尝试在沙箱内自动运行而无需审批，不兼容的命令退回常规审批流程）和“常规权限模式”（所有命令即使被沙箱化也必须经过标准审批流程）。
- **文章位置**：*入门* 及其子部分（*前置条件*、*启用沙箱*、*沙箱模式*）。

**4. 细粒度配置与逃生舱机制**

- **内容**：
  - **配置路径权限**：可以通过配置文件（`settings.json`）中的 `sandbox.filesystem.allowWrite` 赋予子进程对特定外部路径（如 `~/.kube`）的写入权限。不同作用域（如托管、个人）中的配置数组会被合并。也可使用 `denyWrite`, `denyRead`, `allowRead` 等进行精细控制。路径支持 `/`（绝对）、`~/`（主目录相对）、`./`（项目根目录相对）等前缀。
  - **兼容性与逃生舱**：某些工具（如 Docker）不兼容沙箱内运行，可放入 `excludedCommands` 中强制在沙箱外运行（或如 jest 需要禁用 watchman）。当沙箱命令失败时，Claude 有一个有意设计的“逃生舱（escape hatch）”，可使用 `dangerouslyDisableSandbox` 参数在沙箱外重试命令（需走常规权限审批）。可通过设置 `"allowUnsandboxedCommands": false` 彻底禁用此逃生舱。
- **文章位置**：*配置沙箱* 及其子部分（*向特定路径授予子进程写入访问权限*）。

**5. 安全优势与潜在限制**

- **内容**：
  - **安全优势**：防止提示注入（即使代理被操纵也无法修改关键文件如 `~/.bashrc` 或泄露数据到未授权域名）、减少攻击面（如防范恶意 npm 包）、并提供透明的网络拦截通知。
  - **安全限制（风险点）**：网络过滤只限制域名而不检查流量（允许宽泛域名如 `github.com` 可能导致数据泄露，或被域名挂靠技术绕过）；允许 Unix Sockets（如 docker.sock）或赋予关键系统目录的写权限可能导致权限提升；Linux 在 Docker 中如果启用 `enableWeakerNestedSandbox` 会大幅削弱安全性。
- **文章位置**：*安全优势*（含 *防止提示注入* 等） 及 *安全限制* 部分。

**6. 与权限系统（Permissions）的关系**

- **内容**：沙箱和权限系统是互补的深度防御层。权限系统控制 Claude 能使用哪些工具（在工具运行前评估），而沙箱提供 OS 级别强制执行，专门针对 Bash 命令及其子进程在运行时进行文件系统和网络层面的限制。
- **文章位置**：*沙箱如何与权限相关* 部分。

**7. 高级用法与开源实现**

- **内容**：组织可配置自定义代理（设定 HTTP/SOCKS 端口）以检查HTTPS流量或与现有安全设施集成。该沙箱运行时已经作为开源 NPM 包（`@anthropic-ai/sandbox-runtime`）发布，供 AI 社区在其他项目（如 MCP 服务器）中使用。
- **文章位置**：*高级用法*（含 *自定义代理配置*）及 *开源* 部分。

**8. 沙箱不涵盖的内容与限制**

- **内容**：沙箱机制仅隔离 Bash 子进程。内置文件工具（Read, Edit, Write）直接受权限系统控制，不通过沙箱运行。在 macOS 上控制桌面的计算机使用工具（Computer use）也是在实际桌面上运行，不受沙箱隔离。此外，沙箱存在极小的性能开销，并且尚未原生支持 Windows。
- **文章位置**：*限制* 及 *沙箱不涵盖的内容* 部分。